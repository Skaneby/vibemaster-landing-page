# Tailwind CLI binary (download with: curl -sL https://github.com/tailwindlabs/tailwindcss/releases/latest/download/tailwindcss-macos-arm64 -o tailwindcss && chmod +x tailwindcss)
tailwindcss

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Logs
*.log

# Temporary
*.tmp
*.swp

# Internal / AI workflow — do not expose to public repo
tasks/
CLAUDE.MD
.claude/
documents/

# Internal design tools (not production pages)
compare.html

# Build tooling (npm install / npx playwright install to restore)
node_modules/
