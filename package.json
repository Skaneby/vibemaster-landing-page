{
  "name": "vibemaster-landing-page",
  "private": true,
  "description": "Build tooling for the Vibemaster static landing page (no runtime dependencies — these are dev-only).",
  "type": "module",
  "scripts": {
    "build:locales": "node scripts/build-locales.mjs",
    "build:og": "node scripts/build-og-image.mjs",
    "pagespeed": "node scripts/pagespeed.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
